language: cpp

compiler:
    - gcc
    - clang

before_install:
    - sudo add-apt-repository -y ppa:team-xbmc/ppa
    - sudo apt-get update -qq
    - sudo apt-get install -qq libsdl2-dev libfreetype6-dev

before_script:
     - mkdir build
     - cd build
     - cmake ..

script: make
